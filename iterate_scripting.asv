graph_options = [0,0,0,0]

% ID = 6
% make_graphs_4(ID, graph_options)


% ID = 1
% while ID <=  27
%     make_outputs_4(ID, graph_options)
%     ID = ID + 1;
% end

% m = [3,12]
% for i = 1:length(m)
%     ID = m(i)
%     %make_graphs_4(ID, graph_options)
%     run_curvefit_2(ID)
% end